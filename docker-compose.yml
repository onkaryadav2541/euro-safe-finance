version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: eurosafe_db
    environment:
      POSTGRES_DB: eurosafe
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: always

volumes:
  postgres_data: